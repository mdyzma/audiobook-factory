# Hard ceilings for the narrator environment. Passed to every pip install with
# `-c` so pip can resolve transitive dependencies without ever lifting numpy
# off 1.x, which is what breaks XTTS at inference.
numpy==1.26.4
pandas==1.5.3
# spaCy's compiled core must stay on the numpy 1.x line. spacy 3.8 requires
# thinc 8.3.x, which requires numpy>=2 - so the whole stack is held at 3.7.
spacy==3.7.5
thinc==8.2.5
blis==0.7.11
confection==0.1.5
# gruut 2.2.3 predates networkx 3.
networkx==2.8.8
# TTS 0.22.0 imports BeamSearchScorer, removed in transformers 5.x.
transformers==4.40.2
tokenizers==0.19.1
# transformers 4.40 requires the pre-1.0 hub client.
huggingface-hub==0.23.5
